let str1 = "Javascript";
let str3 = "legal !";


console.log(`${str1 + " " + str3}`);

let bool1 = true;

const getValue = (num) => {

    return num + num;
}

console.log(getValue(8)); // Aqui estamos usando o template literals, pra fazer somas com a fução getValue.